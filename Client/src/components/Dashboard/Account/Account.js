import React from "react";
import Info from "./info";
function Account() {
  return (
    <div>
      {/* this page is where the freelancer builds it is profile. so this page all it 
      does is inputs that sends or edits information to the data base */}
      <Info />
    </div>
  );
}

export default Account;
